{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "Dependency",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "The schema version of the dependency JSON entry"
    },
    "name": {
      "type": "string",
      "description": "A unique name of the dependency"
    },
    "url": {
      "type": "string",
      "description": "A link to the dependency, ideally to the GitHub repository to fetch the latest version"
    },
    "description": {
      "type": "string",
      "description": "A short description of the dependency"
    },
    "license": {
      "type": "string",
      "description": "The license of the dependency"
    },
    "language": {
      "type": "string",
      "description": "The programming language of the dependency"
    },
    "community": {
      "type": "string",
      "description": "A link to interact with the community of the dependency, e.g., a link to the Slack or Discord channel"
    }
  },
  "required": ["name", "url", "$schema", "language"],
  "additionalProperties": false
}
